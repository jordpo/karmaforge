<!DOCTYPE html>
<html>
<head>
  <title>KarmaForge</title>
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href='http://fonts.googleapis.com/css?family=Shadows+Into+Light' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Marvel' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application" %>
  <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <div class="navbar navbar-inverse">
      <div class="container-fluid">
        <span id="icon-home"><%= link_to 'KF', root_path %></span>
        <div class="navbar-header pull-right">
         <!-- User NAV -->
         <span class="login-nav">
          <% if user_signed_in? %>
            <span>Signed in as <%= current_user.email %> with <span id="user_pts_container">Total Karma Points:
                <span id="user_pts"><%= current_user.total_points %></span></span>
            </span>
            <span><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></span>
          <% else %>
            <span><%=  link_to 'Sign In', new_user_session_path %></span>
            <span><%= link_to 'Facebook Sign In', user_omniauth_authorize_path(:facebook) %></span>
          <% end %>
         </span>
        </div>
      </div>
    </div>
    <div class="jumbotron">
      <h1 id="title">KarmaForge</h1>
        <h3 id="tagline">Give your old things a new life and boost your community's <span id="karma"> Karma</span>!</h3>
    </div>
    <div class="notice-alert">
      <!-- Notice and Alerts -->
      <% if notice.present? %>
        <p class="notice well"><%= notice %></p>
      <% end %>
      <% if alert.present?%>
        <p class="alert well"><%= alert %></p>
     <% end%>
    </div>
    <%= yield %>
  </div>
</body>
</html>
